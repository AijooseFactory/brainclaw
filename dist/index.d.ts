/**
 * BrainClaw Plugin for OpenClaw
 *
 * Provides hierarchical Leiden community detection,
 * immutable audit ledgers, and hybrid search (Vector + BM25 + Graph).
 */
export default function hybridGraphRagPlugin(api: any): void;
